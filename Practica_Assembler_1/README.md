#Instrucciones b√sicas para tener a mano

- Ensamblar: `nasm -f elf64 -g -F DWARF ejemplo.asm`
- Linkear: `ld -o ejemplo ejemplo.o`
- Ejecutar: `./ejemplo`
- Iniciar gdb `gdb ejemplo`

Reemplazar ejemplo por el nombre del archivo.


